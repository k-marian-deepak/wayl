name: Fullstack CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  backend-ci:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend

    steps:
      - name: Checkout Code
        uses: actions/checkout@4 # actions/checkout@v4.1.7

      - name: Set up Python
        uses: actions/setup-python@5 # actions/setup-python@v5.1.1
        with:
          python-version: '3.10'
          cache: 'pip'
          cache-dependency-path: backend/requirements.txt

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Test Backend Imports
        run: |
          python -c "import main; print('FastAPI app imports successfully')"

  frontend-ci:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend

    steps:
      - name: Checkout Code
        uses: actions/checkout@4 # actions/checkout@v4.1.7

      - name: Set up Node.js
        uses: actions/setup-node@4 # actions/setup-node@v4.0.3
        with:
          node-version: 20

      - name: Cache Node Modules
        id: node-cache
        uses: actions/cache@4 # actions/cache@v4.0.2
        with:
          path: frontend/node_modules
          key: ${{ runner.os }}-node-${{ hashFiles('frontend/package.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Install Dependencies
        if: steps.node-cache.outputs.cache-hit != 'true'
        run: npm install

      - name: Lint Code
        run: npm run lint
        continue-on-error: true

      - name: Build Bundle
        run: npm run build
